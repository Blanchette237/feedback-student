export class Section {/*
    SectionId: number | null;
    description: string;
    title: string;
    createdDate: Date;
    updatedDate: Date;
    dividedinto: DividedInto[];
    question: Question[];


    constructor(section: Partial<Section> = {}) {
      this.SectionId = section?.SectionId || null;
      this.description = section?.description || '';
    }
      */
  }
